#include <iostream>
using namespace std;



bool IsPalindrome(int arr[] , int arrLength , int start = 0 , int end = 0)
{

    
    if(end == 0)
        end = arrLength-1;

    if(start >= end)
        return true;
    
    if(arr[start] != arr[end])
        return false;
    
    return IsPalindrome(arr, arrLength , start+1 , end-1);
}



int main (){
    
    int arr[5] = {10,14,1,14,10};
    cout << IsPalindrome(arr,5) << endl;

    
    return 0;
}
